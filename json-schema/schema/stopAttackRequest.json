{
  "$id": "https://schema.steadybit.io/attack-kit/stopAttackRequest",
  "title": "Start Attack Request",
  "description": "The HTTP request payload passed to the attack stop endpoints.",
  "type": "object",
  "properties": {
    "state": {
      "type": "object",
      "description": "The attack state as returned by the prepare or start attack endpoint."
    },
    "canceled": {
      "type": "boolean",
      "description": "Whether the attack was canceled by the user."
    }
  },
  "required": [
    "state",
    "canceled"
  ]
}