{
  "$id": "https://schema.steadybit.io/attack-kit/attackStatusRequest",
  "title": "Attack Status Request",
  "description": "The HTTP request payload passed to the attack status endpoints.",
  "type": "object",
  "properties": {
    "state": {
      "type": "object",
      "description": "The attack state as returned by the prepare, start or previous state endpoint call."
    }
  },
  "required": [
    "state"
  ]
}